import { MyButton } from './my-button';

describe('my-button', () => {
  it('builds', () => {
    expect(new MyButton()).toBeTruthy();
  });

  // describe('formatting', () => {
  //   it('button click', () => {
  //     const component = new MyButton();
  //     expect(component.handleClick(e) => {e}).toEqual();
  //   });
  // });
});
